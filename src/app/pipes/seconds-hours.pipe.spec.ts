import { SecondsHoursPipe } from './seconds-hours.pipe';

describe('SecondsHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
