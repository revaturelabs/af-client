import { BuildingRequestDto } from './building-request-dto';

describe('BuildingRequestDto', () => {
  it('should create an instance', () => {
    expect(new BuildingRequestDto()).toBeTruthy();
  });
});
